#!/usr/bin/python3
"""Defines a function"""


def is_same_class(obj, a_class):
    """checks if two classes are the same"""
    if type(obj) is a_class:
        return True
    else:
      return False
